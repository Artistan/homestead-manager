en:
  homestead_manager:
    action:
      update_guests: "[homestead-manager:guests] Updating hosts file on active guest virtual machines..."
      update_guest: "[homestead-manager:guest] Updating hosts file on the virtual machine %{name}..."
      update_host: "[homestead-manager:host] Updating hosts file on your workstation (password may be required)..."
    config:
      not_a_bool: "[homestead-manager:config:error] A value for %{config_key} can only be true or false, not type '%{value}'"
      not_an_array_or_string: "[homestead-manager:config:error] A value for %{config_key} must be an Array or String, not type '%{is_class}'"
      not_a_proc: "[homestead-manager:config:error] A value for %{config_key} must be a Proc, not type '%{is_class}'"
